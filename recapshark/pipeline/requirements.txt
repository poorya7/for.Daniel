yt-dlp==2026.3.17
openai==2.26.0
fastapi==0.135.1
uvicorn==0.41.0
subs_provider==1.6.0
httpx==0.28.1
langdetect==1.0.9
slowapi==0.1.9
requests==2.33.1
google-analytics-data==0.21.0
google-cloud-bigquery==3.41.0
# spaCy: NER for transcript name highlighting + all-caps recasing.
# After installing this package, also run on the server:
#   python -m spacy download en_core_web_sm
# spaCy stays inert at runtime unless ENABLE_NER=true in .env, so this
# dependency is safe to install ahead of any rollout.
spacy==3.8.14
# Sentry: error monitoring (lazy karaoke prep). FastAPI integration auto-
# attaches when sentry_sdk.init() runs before app creation. SDK stays inert
# at runtime unless SENTRY_DSN_BACKEND is set in .env (see server.py).
sentry-sdk[fastapi]==2.58.0
# bgutil POT-token yt-dlp plugin (lazy karaoke). Auto-discovers the bgutil
# Node POT server on 127.0.0.1:4416; if the server isn't running (e.g.
# local dev), yt-dlp falls back to non-PO-Token behavior cleanly. The
# Node server itself runs as a separate pm2 process.
bgutil-ytdlp-pot-provider==1.3.1

# ---------------------------------------------------------------------------
# Reproducibility note
# ---------------------------------------------------------------------------
# The list above is direct dependencies only. For a fully-pinned, reproducible
# install (every transitive package locked to the exact version that runs in
# production), use `requirements-lock.txt` instead:
#     pip install -r requirements-lock.txt
# Regenerate the lock after upgrading anything in this file:
#     pip install -r requirements.txt && pip freeze > requirements-lock.txt
