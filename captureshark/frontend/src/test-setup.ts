/**
 * Vitest global setup.
 *
 * Loaded once per test process via `vitest.config.ts`. Adds DOM matchers
 * (`toBeInTheDocument`, etc.) and any other cross-test utilities we need.
 */

import "@testing-library/jest-dom/vitest";
